- add perfect pipes support
- add "xe -h" and "xe --help" (and just good help, too!)
- add mb/s speed to output (and refine english there)
- use 2 buffers: read and operation?
  + don't use built-in buffering?
- extensive useability test
+ use faster memory block?
+ different exception handling system?
- use const FILE*?
- replace LONG_MAX with SIZE_T_MAX?
+ bad encryption of large block containing same chars
  - write custom random function?
